# General Coding Standards
- Language: Python 3.8+
- Styling: PEP8 compliant.
- Docstrings: Required for all public functions, classes, and modules.
- Configuration: API keys and sensitive paths must be stored in `config/config.ini` or environment variables, never hardcoded.
