# Hybrid Model
- Combining: Lexicon features + RoBERTa embeddings + RAG contextual features
- Classifier: `HybridSentimentClassifier` (neural network dense classification layer)
- Evaluation: accuracy, precision, recall, F1, AUC-ROC metrics
- Scripts: `src/hybrid_model.py`, `src/model_evaluation.py`
