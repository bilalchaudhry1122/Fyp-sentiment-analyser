# Architectural and Design Patterns
- Config-driven credentials loading (from `config.ini`).
- Flask application factory pattern (`create_app`).
- Model modularity (separate modules for data extraction, lexicons, fine-tuning, RAG system, hybrid combination).
